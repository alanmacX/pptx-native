{
  "name": "pptx-native-engine",
  "private": true,
  "version": "1.0.0",
  "description": "Bundled PPT-native compile engine (html -> native pptx). sharp/pixelmatch are QA-only (visual_qa).",
  "dependencies": {
    "playwright": "^1.45.0"
  },
  "optionalDependencies": {
    "sharp": "^0.35.3",
    "pixelmatch": "^7.2.0"
  }
}